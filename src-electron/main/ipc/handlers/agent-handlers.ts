import { BrowserWindow } from 'electron'
import { DatabaseInstance } from '../../database'

export function registerAgentHandlers(_mainWindow: BrowserWindow, _db: DatabaseInstance): void {
  // Agent handlers will be implemented later
  // Parameters are kept for future implementation
  void _mainWindow
  void _db
}